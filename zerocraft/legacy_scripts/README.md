# Legacy one-off scripts

Early ZeroCraft .bat/.sql scripts run once against the live DB before the zc_pending/zc_applied flow existed.
Their effect is already in `../db-backup/acore_world.sql.gz`; kept for reference only.
